# PostgreSQL Connection
# For local PostgreSQL (Docker):
DATABASE_URL=postgresql://farmfreshbd_user:farmfreshbd_password@localhost:5432/farmfreshbd

# For production PostgreSQL (replace with your actual credentials):
# DATABASE_URL=postgresql://username:password@host:port/database

# Test Database (for testing)
DATABASE_TEST_URL=postgresql://farmfreshbd_user:farmfreshbd_password@localhost:5433/farmfreshbd_test

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=farmfreshbd
DB_USER=farmfreshbd_user
DB_PASSWORD=farmfreshbd_password

# Test Database Configuration
DB_TEST_HOST=localhost
DB_TEST_PORT=5433
DB_TEST_NAME=farmfreshbd_test
DB_TEST_USER=farmfreshbd_user
DB_TEST_PASSWORD=farmfreshbd_password

# JWT Secret (change this in production!)
JWT_SECRET=your-secret-key-change-this-in-production-use-long-random-string

# Server Port (for local development)
PORT=8000

# Environment
NODE_ENV=development

# CORS: comma-separated list of allowed origins (optional)
# Example for production: set to your Vercel frontend domain(s)
# CORS_ALLOW_ORIGINS=https://your-frontend.vercel.app,https://another-frontend.vercel.app
