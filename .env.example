# MongoDB Connection
# For local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/farmfreshbd

# For MongoDB Atlas (replace <db_password> with your actual password):
MONGODB_URI=mongodb+srv://username:<db_password>@cluster.mongodb.net/farmfreshbd?retryWrites=true&w=majority

# JWT Secret (change this in production!)
JWT_SECRET=your-secret-key-change-this-in-production-use-long-random-string

# Server Port (for local development)
PORT=8000

# Environment
NODE_ENV=development
